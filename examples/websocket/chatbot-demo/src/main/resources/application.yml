spring:
  ai:
    anthropic:
      api-key: ${ANTHROPIC_API_KEY}
      chat:
        options:
          model: claude-sonnet-4-5-20250929
    agent:
      serve:
        websocket:
          allowed-origins:
            - "http://localhost:*"
